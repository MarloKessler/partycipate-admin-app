# Setup
1. You need to have node and npm installed (yarn is recommended).
2. Go to the root folder of this web component.
3. Execute `npm install` or `yarn install`
4. Execute `npm run start` or `yarn start` to start the development server.
5. To build, enter `npm run build` or `yarn build`.